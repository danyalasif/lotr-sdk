{
  "name": "sdk-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "npm run clean && tsc && tsc --build tsconfig.es5.json",
    "clean": "rm -rf dist/lib",
    "test:watch": "jest --watchAll",
    "example": "tsc example/server.ts && node example/server.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "jest": "^29.4.3",
    "ts-jest": "^29.0.5",
    "typescript": "^4.9.5"
  }
}
